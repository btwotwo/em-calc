* Usage
The calculator accepts expressions via ~stdin~:
#+begin_src bash
echo '2 / (3 - 1)' | ./bin/calculator
#+end_src

* Used gems
- ~propcheck~ - for property-based tests
- ~solargraph~ - LSP server
